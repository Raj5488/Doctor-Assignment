                //  <-:Syntax:->
//      Array.forEach(callback(item, index, array)=> Statements);

// => callbackFunctions:- This arguments contains the method that will be called for each array element. it is a mandatory parameters.
//  => items:- The array item on iteration.
//  => index:- This parameter is optional and contains the index number of each element.
//  => array:- This parameters is optional and contains the entire array on which the methods is being applied.



// let itemInCart = ["apple", "comb", "mike", "keyboard", "t-shirt", "Mobile holder"];
// // Display the items in cart
// itemInCart.forEach((item)=> console.log(item));
// // Point:->  Now let's try to use all the parameters. Let's print the item name, the position in which the item was added to the cart, and the total cart items
// itemInCart.forEach((item,index, arr)=>console.log(
//     "The item" item "was added to cart in position"{

//     }
// );)





                        // Syntex
    // The map metho can be applied to an array in the following ways.
    //  1. Function declaration.
    //  2. Arrow Function
    //  3. Callback Functions


                    // Function Declaration
    //   Array.map(function(item, index, array){function Body})

                // Arrow function
    //    array.map((item, index, array) ==>{function Body})


    //  array.map(callback)
//  * item
